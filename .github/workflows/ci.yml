name: CI

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: self-hosted

    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Restore
        run: dotnet restore

      - name: Build
        run: dotnet build --no-restore --configuration Release

      - name: Build Docker images
        run: docker compose build

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: List Docker images
        run: docker image ls

      - name: Tag Docker images
        run: |
          docker tag microservicessample-productservice:latest ghcr.io/${{ github.repository_owner }}/microservicessample-productservice:latest
          docker tag microservicessample-orderservice:latest ghcr.io/${{ github.repository_owner }}/microservicessample-orderservice:latest
          docker tag microservicessample-notificationservice:latest ghcr.io/${{ github.repository_owner }}/microservicessample-notificationservice:latest
          docker tag microservicessample-apigateway:latest ghcr.io/${{ github.repository_owner }}/microservicessample-apigateway:latest

      - name: Push Docker images
        run: |
          docker push ghcr.io/${{ github.repository_owner }}/microservicessample-productservice:latest
          docker push ghcr.io/${{ github.repository_owner }}/microservicessample-orderservice:latest
          docker push ghcr.io/${{ github.repository_owner }}/microservicessample-notificationservice:latest
          docker push ghcr.io/${{ github.repository_owner }}/microservicessample-apigateway:latest


      - name: Deploy
        run: |
          docker compose -f compose.prod.yml pull
          docker compose -f compose.prod.yml up -d
